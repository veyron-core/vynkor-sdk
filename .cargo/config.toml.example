# Copy to .cargo/config.toml (gitignored) to build against an unpublished
# local vynkor-wire checkout, e.g. while both repos are being developed
# together before the next crates.io release.
[patch.crates-io]
vynkor-wire = { path = "../vynkor-wire" }
